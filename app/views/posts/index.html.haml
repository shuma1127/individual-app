.header
  .header__name
    usernameの料理
  .header__posts
    .header__log
      ログイン
    .header__btn
      投稿
.contents
  .search
    %form
      %input.text-field{:type => "text"}/
      %button.submit{:type => "submit"} 検索
  .content
    - @posts.each do |post|
      .content__box{style: "background-image: ur(#{post.image});"}
        .more
          %span= image_tag 'arrow_top.png'
          %ul.more_list
            %li= link_to '削除', "/posts/#{post.id}", method: :delete

        %p.content__box__title
          = post.title
        %span.content__box__word
          = post.word

    .content__box
    .content__box
    .content__box
    .content__box
    .content__box
    .content__box
    .content__box
.footer